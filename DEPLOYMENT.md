# 🚀 Инструкция по развертыванию

## Шаг 1: Сборка проекта

```bash
# Установить зависимости
npm install

# Собрать проект
npm run build
```

После выполнения команды `npm run build` создастся папка **`dist/`** со всеми необходимыми файлами.

## Шаг 2: Проверка локально

```bash
# Запустить локальный сервер для проверки
npx serve dist/
```

Откройте http://localhost:3000 в браузере. Если всё работает - можно загружать на хостинг.

## Шаг 3: Загрузка на хостинг

### ⚠️ ВАЖНО: Загружайте ТОЛЬКО содержимое папки `dist/`

### Вариант A: Обычный хостинг (Shared Hosting, Apache, Nginx)

**Что загружать:**
- Все файлы и папки из `dist/`
- Файл `.htaccess` (для Apache) уже включен в dist/

**Структура после загрузки на хостинг:**
```
public_html/  (или www/, htdocs/)
├── index.html
├── .htaccess
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── [изображения]
└── ...
```

**Для Nginx** добавьте в конфиг:
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### Вариант B: Vercel (Рекомендуется)

```bash
npm install -g vercel
vercel --prod
```

Или подключите GitHub репозиторий через https://vercel.com

### Вариант C: Netlify

**Простой способ:**
1. Перейдите на https://app.netlify.com/drop
2. Перетащите папку `dist/` в окно браузера

**Или через CLI:**
```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

### Вариант D: GitHub Pages

```bash
npm run build
npx gh-pages -d dist
```

## Проверка после развертывания

✅ Главная страница открывается  
✅ Переходы между страницами работают  
✅ Прямые ссылки (например, /product/1) не дают 404  
✅ Изображения загружаются  
✅ Стили применяются корректно  

## Устранение проблем

### Проблема: "Пустая страница"
- Убедитесь, что загружены файлы из `dist/`, а не из корня проекта
- Проверьте консоль браузера (F12) на наличие ошибок

### Проблема: "404 при переходе на другие страницы"
- Убедитесь, что файл `.htaccess` (для Apache) присутствует
- Для Nginx добавьте конфигурацию `try_files`
- Для других серверов настройте редирект всех запросов на index.html

### Проблема: "Не загружаются стили/скрипты"
- Проверьте правильность путей в index.html
- Убедитесь, что папка `assets/` загружена полностью

## Полезные ссылки

- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [React Router Deployment](https://reactrouter.com/en/main/start/tutorial#deploying)
